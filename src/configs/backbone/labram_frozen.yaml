# @package backbone
name: labram_frozen
n_channels: 64
trial_seconds: 4.0
freeze: true
batch_size: 32
n_classes: 4
pretrained_id: braindecode/Labram-Braindecode
input_scale: 1.0e6   # MNE returns volts; pretrained LaBraM expects microvolt-scale EEG.
feature_pool: channel_mean
use_cls_token: false
