# @package head
# Backwards-compat alias for softmax_probe — left in place because experiment
# configs still reference linear_probe by name.
name: linear_probe
C: 1.0
max_iter: 1000
calibrate_on_calib: true
