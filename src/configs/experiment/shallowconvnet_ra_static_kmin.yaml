# @package _global_
defaults:
  - override /backbone: shallowconvnet
  - override /protocol: kmin_calibration
adapters:
  - name: ra_static
    regularize: 1.0e-5
    target_space: identity
    zero_shot_fallback: per_trial
