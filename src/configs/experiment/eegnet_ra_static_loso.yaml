# @package _global_
defaults:
  - override /backbone: eegnet
  - override /protocol: loso
adapters:
  - name: ra_static
    regularize: 1.0e-5
    target_space: identity
    zero_shot_fallback: per_trial
dataset:
  per_trial_zscore: false
