# @package _global_
defaults:
  - override /backbone: labram_frozen
  - override /protocol: loso
head:
  name: linear_probe
  C: 1.0
adapters:
  - name: ra_static
    regularize: 1.0e-5
    target_space: identity
    zero_shot_fallback: per_trial
